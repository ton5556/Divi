# 🚀 ESP32 Motor Control via Serial

โปรเจกต์นี้เป็นการควบคุมมอเตอร์ DC ด้วย **PWM** บน **ESP32** ผ่าน Serial Monitor โดยสามารถกำหนดค่า Pin, ความถี่, และ Duty Cycle ได้เองแบบ Real-time

---

## 📋 สารบัญ

- [คุณสมบัติ](#-คุณสมบัติ)
- [อุปกรณ์ที่ต้องใช้](#-อุปกรณ์ที่ต้องใช้)
- [การติดตั้ง](#-การติดตั้ง)
- [วิธีใช้งาน](#-วิธีใช้งาน)
- [ตัวอย่างการใช้งาน](#-ตัวอย่างการใช้งาน)
- [โครงสร้างโค้ด](#-โครงสร้างโค้ด)

---

## ✨ คุณสมบัติ

- ✅ กำหนด **GPIO Pin** ที่ต้องการใช้ได้เอง
- ✅ กำหนด **ความถี่ PWM** (1 - 40,000 Hz)
- ✅ ปรับ **Duty Cycle** (0 - 255) แบบ Real-time
- ✅ กด `e` เพื่อ **รีเซ็ตค่าทั้งหมด** และเริ่มกรอกใหม่
- ✅ มีการตรวจสอบค่าที่กรอกและแจ้งเตือน Error

---

## 🛠 อุปกรณ์ที่ต้องใช้

| อุปกรณ์ | รายละเอียด |
|--------|-----------|
| ESP32 | Development Board |
| มอเตอร์ DC | ขนาดตามต้องการ |
| Driver | 2n2219 |
| สายไฟ | สำหรับต่อวงจร |

---

## 📦 การติดตั้ง

### 1. ติดตั้ง MicroPython บน ESP32

ดาวน์โหลด MicroPython firmware ได้ที่ [micropython.org](https://micropython.org/download/ESP32_GENERIC/) แล้วแฟลชลง ESP32

```bash
esptool.py --port COM3 erase_flash
esptool.py --port COM3 write_flash -z 0x1000 esp32-firmware.bin
```

### 2. อัปโหลดไฟล์โค้ด

อัปโหลดไฟล์ `main.py` ไปยัง ESP32 ด้วยเครื่องมือเช่น **Thonny IDE** หรือ **ampy**

```bash
ampy --port COM3 put main.py
```

---

## 🎮 วิธีใช้งาน

เปิด Serial Monitor (Baud Rate: **115200**) แล้วทำตามขั้นตอน:

```
=== ตั้งค่าใหม่ ===
กรุณาใส่หมายเลข Pin (เช่น 12):
> 12
✅ Pin = 12

กรุณาใส่ความถี่ Hz (เช่น 1000):
> 1000
✅ ความถี่ = 1000 Hz

กรุณาใส่ Duty Cycle 0-255:
> 128
✅ Duty Cycle = 128

🚀 มอเตอร์ทำงานที่ Pin=12, Freq=1000Hz, Duty=128
พิมพ์ตัวเลข 0-255 เพื่อเปลี่ยน Duty | พิมพ์ e เพื่อรีเซ็ต
```

### คำสั่งที่ใช้ได้

| คำสั่ง | ผลลัพธ์ |
|-------|--------|
| `0` - `255` | ปรับ Duty Cycle (ความเร็วมอเตอร์) |
| `e` | รีเซ็ตค่าทั้งหมดและเริ่มกรอกใหม่ |

### ช่วงค่าที่รับได้

| พารามิเตอร์ | ค่าต่ำสุด | ค่าสูงสุด |
|-----------|---------|---------|
| GPIO Pin | 0 | 39 |
| ความถี่ (Hz) | 1 | 40,000 |
| Duty Cycle | 0 | 255 |

---

## 💡 ตัวอย่างการใช้งาน

**ควบคุมความเร็วมอเตอร์:**
- `0` → มอเตอร์หยุด
- `128` → มอเตอร์ทำงาน 50%
- `255` → มอเตอร์ทำงาน 100%

---

## 🗂 โครงสร้างโค้ด

โค้ดทำงานด้วยระบบ **State Machine** แบ่งเป็น 4 สถานะ:

```
STATE_PIN  → รับค่า GPIO Pin
STATE_FREQ → รับค่าความถี่ PWM
STATE_DUTY → รับค่า Duty Cycle เริ่มต้น
STATE_RUN  → มอเตอร์ทำงาน / ปรับ Duty แบบ Real-time
```

---

## 📄 License

T.RODBUMRUNG
